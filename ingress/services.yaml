apiVersion: v1
kind: Service
metadata:
  name: apple-home-service
  labels:
    app: my-svc-home
    env: devops
spec:
  selector:
    app: cbz-home
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80

