apiVersion: v1
kind: ReplicationController
metadata:
  name: myreplicationC
  labels:
   app: myapp
spec: 
 template:
   metadata:
     name: myreplicationC
     labels:
       app: myapp
   spec:
      containers:
        - name: httpd
          image: httpd
          ports:
            - name: httpd

