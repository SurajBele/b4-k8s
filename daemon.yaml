apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: monitoring
  labels:
    app: myapp
    tier: monitoring
spec:
  replicas: 3
  selector:
    matchLabels:
      tier: onitoring
  template:
    metadata:
      labels:
        tier: onitoring
    spec:
      containers:
        - name: myapp
          image: nginx
          ports:
            - containerPort: 80
