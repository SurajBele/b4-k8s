apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: frontend-myreplicaset
  labels:
    app: myapp
    tier: frontend
spec:
  
  selector:
    matchLabels:
      tier: frontend
  template:
    metadata:
      labels:
        tier: frontend
    spec:
      containers:
        - name: myapp
          image: nginx
          ports:
            - containerPort: 80
