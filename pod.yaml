apiVersion: v1
kind: Pod
metadata:
  name: mynginx
  labels: 
    app: myapp
spec:
 containers:
   - name: mynginx
     image: nginx:latest
     ports:
      - name: httpd
        containerPort: 80
      - name: nginxport
        containerPort: 8080
   - name: apche2
     image: apache2
